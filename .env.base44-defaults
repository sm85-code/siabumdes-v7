# Development placeholders — overridden by /run/base44/app.env (platform secrets).
# This file lets the app boot before real secrets are configured.
JWT_SECRET=dev-placeholder-jwt-secret-not-for-production-use
