{
  "previewPort": 3000,
  "healthPath": "/",
  "startCommand": "docker compose -f docker-compose.base44.yml up -d",
  "secrets": [
    {
      "name": "JWT_SECRET",
      "description": "Secret key used to sign JWT auth tokens. Generate a long random string.",
      "requiredAtBoot": true,
      "generatedForDevelopment": true
    }
  ]
}
